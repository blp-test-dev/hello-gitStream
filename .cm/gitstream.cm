# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  get_repo_file:
    if:
      - true
    run:
      - action: add-comment@v1
        args:
          comment: |
            {{ content }}


content: {{ repo | getRepoFile(path='README.md', auth="{{ env.GITSTREAM_TOKEN }}") | dump }}
