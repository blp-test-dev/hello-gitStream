# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  banana_check:
    if:
      - {{ pr.author | match(list={{ gitstreamUsers }}) | some }}
    run:
      - action: add-comment@v1
        args:
          comment: |
            {{ pr.author }} is a gitStream user!
