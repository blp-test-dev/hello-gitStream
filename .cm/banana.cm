# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  is_gitstream_user:
    if:
      - {{ pr.author | gitstreamUsers | nope }}
    run:
      - action: add-comment@v1
        args:
          comment: {{ pr.author }} is not a gitStream user.
